# nginx-quic
This directory contains patches and resources (like `requirements.txt`) used by the main `Dockerfile` in the repository root to build Nginx with HTTP/3 support.
It is no longer a standalone Docker build context.

