{
	"type": "object",
	"description": "Health object",
	"additionalProperties": false,
	"required": [
		"status"
	],
	"properties": {
		"status": {
			"type": "string",
			"description": "Healthy",
			"example": "OK"
		},
		"setup": {
			"type": "boolean",
			"description": "Whether the initial setup has been completed",
			"example": true
		},
		"version": {
			"type": "string",
			"description": "The version string",
			"example": "..."
		},
		"demo": {
			"type": "boolean",
			"description": "Whether demo restrictions are enabled"
		},
		"csrfToken": {
			"type": "string",
			"description": "CSRF token for subsequent browser requests"
		}
	}
}
