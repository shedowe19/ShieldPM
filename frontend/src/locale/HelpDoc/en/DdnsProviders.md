## What is DDNS?

Dynamic DNS (DDNS) allows you to automatically update a domain name with your changing, dynamic IP address. This ensures your services remain accessible via your domain name even if your ISP changes your IP address.

ShieldPM periodically checks your public IP and updates your DNS provider if a change is detected.